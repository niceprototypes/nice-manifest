# Session 2026-04-22

**User:** Add `backgroundSize` as a core token set — variants `contain` / `cover` / `fill` / `none` / `scale-down`.

**Claude (17:54, styles):** Added `backgroundSize` to `src/tokens/module.json`, ran `npm run build` to regenerate. After the build, `src/generated/tokensData.ts`, `src/generated/types.ts` (`BackgroundSizeType = "contain" | "cover" | "fill" | "none" | "scale-down"`), `dist/variables.css`, and `dist/css/backgroundSize.css` all emit the new variants. Added `BackgroundSizeType` to the explicit type re-export list in `src/index.ts`.

**User:** Expose the new `BackgroundSizeType` from nice-styles so nice-react-image can consume it.

**Claude (17:57, react-styles):** Added `BackgroundSizeType` to the explicit `export type { … } from 'nice-styles'` list in `src/index.ts` (alphabetical slot between `BackgroundColorType` and `BorderColorType`). Rebuilt — `dist/index.d.ts` now includes `BackgroundSizeType` in the re-export list.

**User:** Consume the new `backgroundSize` core token set so the prop works like `borderRadius` — type comes from nice-styles, runtime resolves via `getToken`.

**Claude (17:57, react-image):** `src/components/Image/types.ts` now imports `BackgroundSizeType` from `nice-react-styles` and sets `ImageBackgroundSizeType = BackgroundSizeType` (the union is unchanged: `contain | cover | fill | none | scale-down`). In `src/components/Image/styles.ts`, the `<img>` `object-fit` rule now reads `getToken("backgroundSize", $backgroundSize, $mode).var`, mirroring the `borderRadius` pattern. The div `background-size` rule still uses the local `backgroundSizeToCss` helper because CSS `background-size` rejects `fill` / `none` / `scale-down`; that helper now maps `none` → `auto` (previously it emitted invalid `none`). Ran `npm run build`; `dist/index.d.ts` confirms `ImageBackgroundSizeType = BackgroundSizeType`.

**User:** Keep the Token Groups table in sync with the new `backgroundSize` core token set.

**Claude (17:58, manifest):** Added a `backgroundSize` row to `read/styles/tokens.md` → `contain, cover, fill, none, scale-down`, between `backgroundColor` and `borderColor`. The actual token lives in `nice-styles` (source JSON + regenerated types/CSS); this doc only tracks the canonical variant list.

**User:** Factor every hardcoded `https://picsum.photos/…` URL behind `getPicsum(width, height?, id?)` — only `width` is required. When `height` is omitted the image is square (height defaults to width); `id` defaults to a random value. Only create a `DEMO_IMAGE` constant in a story when there are multiple usages; with a single usage, inline the call. Image stories pick their own random id per module; Lightbox stories keep id=10.

**Claude (20:39, storybook):**
- **Service:** added `src/services/getPicsum.ts` with signature `getPicsum(width: number, height: number = width, id: number = Math.floor(Math.random() * 1000)): string`; re-exported from `src/services/index.ts`. Literal URL template now lives only in the service body.
- **Image stories (random id per module):**
  - Multiple usages → module-level `DEMO_IMAGE = getPicsum(w, h)`: `Basic`, `Mode`, `As`.
  - Single usage → inline `getPicsum(w, h)` at the call site: `BackgroundSize`, `BackgroundPosition`, `BorderRadius`, `RenderImage`.
- **Lightbox stories (id=10):**
  - `Basic.story.tsx`, `Caption.story.tsx` — `DEMO_IMAGE = getPicsum(800, 600, 10)`.
  - `Lightbox.stories.tsx` — one inline `getPicsum(800, 600, 10)`.

**User:** For each Image story demo, use a single props object that drives both the live `<Image>` and the `<Source>` block, so the Source can never drift from the Image values.

**Claude (20:39, storybook):**
- **Service:** added `src/services/toSource.ts` — `toSource(tag, props, { children?, raw? }): string` serializes a props object to a JSX source string. `children` passes nested content; `raw` passes props whose value is a literal expression (functions, JSX trees) rather than a scalar.
- **Pattern:** every Image MDX demo defines one named props object, spreads it into `<Image {...props} />`, and passes the same object to `toSource("Image", props)` for `<Source>`.
- **Extraction rule:** a literal becomes a named `export const` **only when referenced more than once in the file**; a single-use literal stays inline at its call site. Under this rule:
  - Source (picsum URL) dimensions hardcoded to `800, 600`; display dimensions hardcoded to `"240px"` / `"320px"` — no `WIDTH` / `HEIGHT` constants.
  - `DEMO_IMAGE` stays extracted in `Basic`, `As` (2 refs each).
  - `Mode` puts `getPicsum(800, 600)` directly on `baseProps.src` (shared object spread twice).
  - `sizes` / `positions` / `radii` arrays inlined at their `.map(...)` sites in `BackgroundSize` / `BackgroundPosition` / `BorderRadius`.
  - `RenderImage` keeps `imageProps` (2 refs), inlines `width={240}` on the live `<img>`, and keeps `renderImageExpr` for multi-line template readability.
  - Picsum ids `60`, `69` used once each — inlined at the `getPicsum` call.
- **Special cases:**
  - `As.mdx` demo 2 passes `{ children: "<h1>Overlay content</h1>" }`.
  - `RenderImage.mdx` passes `{ raw: { renderImage: renderImageExpr } }`.
- **Outcome:** seven Image MDX files have zero hand-authored prop duplication between Image and Source, and no named constants used only once.

**User:** Make `backgroundSize` a core token set like `borderRadius` — driven from nice-styles, with runtime resolution through `getToken`.

**Claude (20:39, storybook):**
- **nice-styles:** added `backgroundSize` to `src/tokens/module.json` with variants `contain` / `cover` / `fill` / `none` / `scale-down`; rebuild emitted `BackgroundSizeType` in `src/generated/types.ts` and `--np--background-size--{variant}` CSS vars.
- **Exports:** added `BackgroundSizeType` to the explicit export list in both `nice-styles/src/index.ts` and `nice-react-styles/src/index.ts`.
- **nice-react-image:**
  - `ImageBackgroundSizeType = BackgroundSizeType` re-export.
  - `<img>` styled path now resolves via `object-fit: ${getToken("backgroundSize", $backgroundSize, $mode).var}` — same pattern as `borderRadius`.
  - `<div>` path keeps the runtime translation helper because CSS `background-size` doesn't accept `fill` / `none` / `scale-down`; helper now maps `none` → `auto` (previously mapped to an invalid value silently).
- **Docs:** added `backgroundSize` to the Token Groups table in `nice-manifest/read/styles/tokens.md`.

**User:** Confirm Tile has no required props — it should render as an empty shell if needed.

**Claude (20:39, storybook):** Verified — no change needed. In `nice-react-tile/src/components/Tile/types.ts`, every field on `TileProps` is already optional (`children?`, `title?`, `description?`, `contentTop/Center/Left/Right?`, layout/background/foreground — all `?`). `TileContent.tsx` gates the header block behind `!!title || !!description || !!contentCenter` and passes `children` through unconditionally, so `<Tile />` with no props renders as an empty `Flex` column. Consumers already use this pattern: `nice-website-2025` (`ParagraphTile`, `Contact`, `ProjectHelpShelf`, `Principle`) and several storybook stories (`Basic`, `Spacing`, `BackgroundColor`, `FullWidth`, `MaxWidthDesktop`, `ContentLeft`, `ContentRight`) render `<Tile>` without a `title`.
