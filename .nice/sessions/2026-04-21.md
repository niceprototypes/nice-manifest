# Session 2026-04-21

**User:** I want every future Claude instance to read all of nice-manifest without me having to clarify "read what?" each time.

**Claude (17:58, manifest):** Added a "READ-ALL DIRECTIVE" block at the top of `README.md` with trigger phrases and an explicit numbered Read Manifest listing every file. Mirrored a one-line pointer to the directive in each subdirectory README (`read/`, `read/projects/`, `edit/`, `build/`, `publish/`). Maintenance rule: any new manifest file must be appended to the Read Manifest list in the same commit.

**User:** Every project I work in should maintain a chat-log-style record of our sessions. Make it a folder named `claude.md/` at the project root, with one file per session named `YYYY-MM-DD-NNNN.md`. Alternating **User** (paraphrased) and **Claude** (final understanding) entries. When a question or answer is refined mid-session, refactor the existing entry in place — no refinement history.

**Claude (17:58, manifest):** Added `edit/session-log.md` documenting the `claude.md/` folder convention, file naming, serial rules, chat-log format, paraphrasing/understanding rules, and the refactor-in-place rule. Added the new file to the Read Manifest list in `README.md` (position 13) and to the file index in `edit/README.md`. Removed an earlier duplicative `CLAUDE.md` file from this repo root — its READ-ALL content lives in `README.md`, and the root filename is now reserved for the `claude.md/` session-log folder on case-insensitive filesystems.

**User:** Keep the Token Groups table in sync with the `borderColor` variant rename from `heavy`/`heavier` to `dark`/`darker`.

**Claude (17:58, manifest):** Updated the `borderColor` row in `read/styles/tokens.md` → `base, dark, darker`. The actual rename lives in `nice-styles` (source JSON + regenerated types/CSS); this doc only tracks the canonical variant list.

**User:** Rename the `borderColor` token variants `heavy` / `heavier` to `dark` / `darker`.

**Claude (18:50, styles):** Edited `src/tokens/module.color.json` (both `day` and `night` maps) to rename the variants and ran `npm run build` to regenerate derived artifacts. After the build, `src/generated/colorTokensData.ts`, `src/generated/types.ts` (`BorderColorType = "base" | "dark" | "darker"`), `dist/variables.css`, `dist/color-scheme.css`, and `dist/css/borderColor.css` all emit the new variant names. No hand edits to any generated file.

**User:** Replace the emojis in the Welcome story with `nice-react-icon` instances using the vendor flag (Lucide-backed), and make the icons statically inlined next to each section title — not hover-revealed like Storybook's heading anchor.

**Claude (20:24, storybook):**
- **Files touched:**
  - `stories/Welcome.mdx` — replaced emoji prefixes with `<Icon vendor />` using Lucide names `Palette` (Tokens), `Cable` (Bindings), `Blocks` (Components). Each title rendered as a JSX `<h3>` wrapped in a flex container (`display: flex`, `gap: 0.5rem`) to inline the icon alongside the text.
  - `.storybook/preview.tsx` — side-effect import of `nice-react-icon-vendor` to register the vendor resolver.
  - `package.json` — added `nice-react-icon-vendor` as `file:../nice-react-icon-vendor`.
- **Why JSX heading over markdown:** Storybook addon-docs auto-anchors markdown headings with hover-only chrome, which caused the icon to appear only on hover.
- **Install:** ran `npm install` + `npx nice-toolkit --clean-all` — vendor package symlinked into `node_modules/nice-react-icon-vendor`, singleton conflicts cleaned across 16 linked packages.

**User:** Apply the new project-level `claude.md/` session log process to storybook going forward, scoped to storybook-only work.

**Claude (20:24, storybook):** Trimmed this session file to storybook-scoped entries only. The nice-manifest work (READ-ALL directive, creation of `edit/session-log.md`) has been moved to `~/Code/nice-manifest/claude.md/2026-04-21-0001.md` per the rule that each project owns its own log.

**User:** The Icon story is throwing `Cannot convert undefined or null to object` at `Icon.services.ts:3` because `Theme.fontSize` and `Theme.foregroundColor` no longer exist.

**Claude (20:24, storybook):** `Theme` (= `nice-styles` `tokensData`) was refactored — `fontSize` moved to `sizeTokensData` (breakpoint-keyed: `mobile`/`tablet`/`desktop`) and `foregroundColor` moved to `colorTokensData` (mode-keyed: `day`/`night`), so both are no longer direct keys on `Theme`. Rewrote `stories/Components/Icon/Icon.services.ts` to export the variant arrays as hardcoded literal tuples constrained by `satisfies readonly IconSizeType[]` / `satisfies readonly IconColorType[]` — if either union shifts, TypeScript fails the build. Consumers (`Size.story.tsx`, `Color.story.tsx`) work unchanged.

**User:** Replace the TSX Icon Name story with an MDX docs page that uses Storybook's native `<Source>` doc block, and reorder the row so the name label comes first.

**Claude (20:24, storybook):** Added `stories/Components/Icon/Name.mdx` (`<Meta title="React/Components/Icon/name" />`) that iterates `iconNames` and renders each row as a grid of Icon demo | `<Source language="tsx" …>` — the latter being the MDX-native replacement for the custom `CodeBlock` used by `VariableRow`. Row layout is extracted to `Name.styles.ts` as a styled-component (`Row`) following the storybook `{Component}.styles.ts` convention. Deleted `stories/Components/Icon/stories/Name.story.tsx` and removed its import + `Name` story export from `Icon.stories.tsx`.

**User:** Rename the `borderColor` token variants `heavy` / `heavier` to `dark` / `darker` across the entire ecosystem (storybook + website + all nice-* packages).

**Claude (20:24, storybook):**
- **Source rename:** updated `~/Code/nice-styles/src/tokens/module.color.json` (both `day` and `night` maps) and rebuilt — `generated/types.ts`, `generated/colorTokensData.ts`, `dist/variables.css`, `dist/color-scheme.css` now emit `--np--border-color--{base,dark,darker}` and `BorderColorType = "base" | "dark" | "darker"`.
- **Consumers updated:**
  - `stories/Components/Flex/stories/Mode.story.tsx`
  - `stories/Components/Icon/Name.styles.ts`
  - `stories/Styles/Tokens/getToken/getToken.stories.tsx`
  - `stories/Styles/Browse/Browse.stories.tsx`
  - `~/Code/nice-manifest/read/styles/tokens.md` (Token Groups table)
- **nice-website-2025:** zero usages, no changes needed.
- **Verification:** grep confirmed zero leftover `heavy` / `heavier` references on `borderColor`.
