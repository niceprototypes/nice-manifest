# Session 2026-04-22 #0001

**User:** Keep the Token Groups table in sync with the new `backgroundSize` core token set.

**Claude:** Added a `backgroundSize` row to `read/styles/tokens.md` → `contain, cover, fill, none, scale-down`, between `backgroundColor` and `borderColor`. The actual token lives in `nice-styles` (source JSON + regenerated types/CSS); this doc only tracks the canonical variant list.
